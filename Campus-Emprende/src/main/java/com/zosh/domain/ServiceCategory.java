package com.zosh.domain;

// Enum que define las categorías de servicios disponibles en la plataforma
public enum ServiceCategory {
    
    // Desarrollo web (frontend, backend, fullstack)
    WEB_DEV,
    
    // Diseño gráfico (logos, branding, etc.)
    GRAPHIC_DESIGN,
    
    // Soporte técnico (hardware, software, mantenimiento)
    TECH_SUPPORT,
    
    // Tutorías o clases en distintas áreas
    TUTORING,
    
    // Servicios de fotografía
    PHOTOGRAPHY,
    
    // Categoría genérica para otros servicios
    OTHER
}