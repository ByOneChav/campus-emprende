package com.zosh.domain;

// Enum que define el tipo de entidad que puede ser reportada en el sistema
public enum ReportTargetType {
    
    // Reporte relacionado a un servicio
    SERVICE,
    
    // Reporte relacionado a una reseña (review)
    REVIEW,
    
    // Reporte relacionado a un usuario
    USER
}