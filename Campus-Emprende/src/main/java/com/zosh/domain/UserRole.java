package com.zosh.domain;

// Enum que define los roles de usuario dentro del sistema de seguridad
public enum UserRole {

    // Rol para usuarios estudiantes (acceso limitado a funcionalidades)
    ROLE_STUDENT,

    // Rol con privilegios administrativos (gestión completa del sistema)
    ROLE_ADMIN

}

